---
const { dayNumber, url, isSelectedDay } = Astro.props;
---

<p>
    <a href={url} class:list={[{ selected: isSelectedDay }]}>Day {dayNumber}</a>
</p>

<style>
    a.selected {
        color: var(--crystalBlue);
    }
</style>
